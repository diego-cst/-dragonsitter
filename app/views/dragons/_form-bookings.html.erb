<%= simple_form_for [@dragon, @booking] do |f| %>
  <%= f.input :start_date,  as: :string, wrapper_html: { class: 'booking-start-date' } %>
  <%= f.input :end_date, as: :string %>
  <%= f.submit "Book #{@dragon.name}", class: "button-grey" %>
<% end %>
